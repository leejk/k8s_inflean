apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: ljk197
spec:
  # type: ClusterIP 
  # 생략시 default는 ClusterIP.클러스터 내부에서만 사용
  # NodePort, LoadBalancer등의 타입을 사용시 service를 통해 외부접근가능
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: my-nginx
  